# Frontend build-time environment variables (set in Vercel → Project → Settings → Environment Variables).
# Base URL of the deployed backend API (your Render service URL).
# Leave blank for local dev — Vite proxies /api to http://localhost:8000.
VITE_API_URL=https://your-backend.onrender.com
